# Project: tasks-app

Improvements and next steps for **this task list app** (apps/tasks). No legacy appendix — use **details/** for acceptance criteria and notes.

Examples: project filter, app column/filter, drag-and-drop reorder, in-app editing. Add tasks to `index.yaml` with `project: tasks-app` and `app: tasks`.
